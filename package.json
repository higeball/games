{
  "name": "top-road-moji-gary",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:moji": "vite build",
    "build:neko": "cd neko-nine && (npm ci || npm install) && npm run build",
    "build": "vite build && npm run build:neko && mkdir -p dist/neko-nine && cp -r neko-nine/dist/* dist/neko-nine/",
    "preview": "vite preview",
    "test:neko": "npm --prefix neko-nine test"
  },
  "dependencies": {
    "three": "^0.174.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
